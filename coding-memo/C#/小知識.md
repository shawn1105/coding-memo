#### 1. 大量 Insert 資料進DB可以運用 **SqlBulkCopy** 方法，另外會與 FastMember 的 ObjectReader 組合使用，以最快速度批次插入資料到 SQL Server，省去手動建立 DataTable 的時間。
#### 2. DB若是查詢出來的結果有null值(像是left join等)，**C#的Null不等於DB的Null，要使用判斷DBNull.Value正規化查出來的值**
#### 3. 善用IEnumerable<T>方法，可運用於foreach()及LINQ
---
>`即時資料盡量不進DB`
